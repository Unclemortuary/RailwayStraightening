// const { ConnectionBuilder } = require('electron-cgi');

// const connection = new ConnectionBuilder()
//     .connectTo('dotnet', 'run', '--project', '../Server')
//     .build();

// connection.send("hello", "Artem", (error, response) => {
//     update(response);
//     connection.close();
// });

// function update(message) {
//     const spanEl = document.getElementById('textholder');
//     spanEl.innerHTML = message;
// };

